<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Cardapio_Interativo.Pages.LoginPage"
             Title="">
    <VerticalStackLayout VerticalOptions="Center">
        <Border WidthRequest="600" HeightRequest="700" StrokeShape="RoundRectangle 50">
            <VerticalStackLayout HorizontalOptions="Center">
                <Label Text="Seja Bem vindo" HorizontalTextAlignment="Center" FontSize="32" FontAttributes="Bold" Margin="0,90,0,70"/>

                <VerticalStackLayout WidthRequest="350" Padding="0" Spacing="10" Margin="0,0,0,100">
                    <Entry Placeholder="Email" x:Name="txtEmail"/>
                    <HorizontalStackLayout >
                        <Entry Placeholder="Senha" x:Name="txtSenha" WidthRequest="350"/>
                        <ImageButton/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>

                <Button Text="Acessar" x:Name="btnLogin" Clicked="btnLogin_Clicked" WidthRequest="350"/>
                <Button Text="Acessar2" x:Name="btnLogin2" Clicked="btnLogin_Clicked_1" WidthRequest="350" Margin="20"/>
            </VerticalStackLayout>
        </Border>
    </VerticalStackLayout>
</ContentPage>